# “一句话生成股票分析看板”需求与实施规格

## 1. 文档目的

本文档将 `TASK.md` 转化为可开发、可测试、可验收的项目规格。当前阶段只定义需求、架构和实施计划，不创建项目、不编写业务代码。

项目目标是交付一个本地可运行的笔试 Demo：用户输入一句股票分析需求，系统以确定性方式解析为结构化可视化 Schema，再使用项目内置的模拟数据渲染分析看板，并生成一条与图表数据严格一致的简短结论。

项目成功标准按以下顺序排序：

1. 可运行：按 README 的命令可在本地启动。
2. 可复现：相同输入、相同数据版本始终得到相同 Schema、图表和结论。
3. 可测试：解析、Schema 校验、数据查询和关键页面状态均可自动验证。
4. 错误可解释：非法输入或异常不会导致页面崩溃，用户能知道问题和下一步操作。
5. 展示丰富：在满足以上要求后扩展图表类型和交互体验。

## 2. 范围与关键决策

### 2.1 范围内

- 单页本地 Web Demo。
- 一个自然语言输入框，内置不少于 5 条可直接执行的示例指令。
- 将自然语言解析为结构化 Schema。
- Schema 的展示、校验和基于 Schema 的图表渲染。
- 至少折线图、柱状图两类图表；建议 P1 增加组合图、面积图或散点标注，以体现可视化丰富度。
- 基于图表实际数据生成一条确定性结论。
- 基于上一次成功 Schema 的一次简单追问修改。
- “理解需求 → 生成 Schema → 查询数据 → 渲染图表”的过程状态。
- 内置、固定、可版本化的模拟股票数据。
- 对无效 Schema、未知股票、未知指标、非法时间范围、空数据等情况给出可理解的错误。
- README、Schema 定义与样例、Agent 对话记录、自动化测试。

### 2.2 范围外

- 真实金融行情、投资建议和交易能力。
- 复杂 K 线及复权、财务指标计算、实时更新。
- 多数据源聚合及生产级问财 Skill Hub 接入。
- 自由拖拽布局、用户账户、权限、持久化看板。
- 任意多轮对话记忆；只支持基于最近一次成功结果的一次修改。
- 生产级流式服务、分布式任务、数据库和后端微服务。
- 对任意自然语言的完整理解；P0 只承诺已定义意图和同义表达。

### 2.3 数据源决策

题目同时要求“优先通过问财 Skill Hub 获取数据”和“项目自包含全部运行数据、无需外部行情”。为保证离线可运行和验收稳定，P0 采用内置模拟数据作为默认且唯一必需的数据源，并通过统一 `DataProvider` 接口隔离数据访问。Schema 中明确写入 `dataSource.type = "mock"`、数据版本和截止时间。

问财 Skill Hub 作为 P2 可选适配器：仅在存在可用凭据和稳定调用方式时启用，失败后可明确提示并回退到内置模拟数据；它不得成为项目启动、测试或演示的前置条件。若最终未实现，应在 README 的已知限制中说明原因。

## 3. 用户与核心场景

### 3.1 目标用户

- 主要用户：项目评审者，希望快速验证自然语言解析、Schema 设计、图表渲染和工程质量。
- 次要用户：开发者，希望通过固定样例和测试复现所有主要行为。

### 3.2 核心用户流程

1. 用户打开页面，看到输入框、示例指令和模拟数据声明。
2. 用户输入或选择一条示例指令并提交。
3. 页面依次显示处理状态。
4. 系统解析指令，规范化字段并校验 Schema。
5. 系统按 Schema 从内置数据查询并派生指标。
6. 页面展示 Schema、图表和数据一致的结论。
7. 用户输入一次简单追问；系统在上次成功 Schema 上只修改被指定的字段，重新校验并渲染。
8. 任一步失败时页面保留输入和最近一次成功结果（若有），同时展示错误原因和修复建议。

## 4. 功能需求与验收标准

### FR-01 自然语言输入

- 提供单行或多行输入框、提交按钮，并支持 Enter 提交（多行时可用 Ctrl/Cmd + Enter）。
- 去除首尾空白后才提交；空输入不可进入解析流程。
- 处理期间防止重复提交，或以“最后一次请求为准”取消旧请求。
- 页面内提供至少 5 条可点击示例，覆盖不同股票、指标、时间范围、图表类型和极值标注。

建议固定样例：

1. `分析 A 公司最近 30 天的股价和成交量，并标出跌幅最大的 3 个交易日。`
2. `查看 B 公司最近 10 个交易日的收盘价走势。`
3. `用柱状图展示 A 公司最近 20 个交易日的成交量。`
4. `比较 C 公司最近 15 个交易日的涨跌幅，并标出涨幅最大的 3 天。`
5. `展示 B 公司 2025-03-03 到 2025-03-28 的收盘价和成交量。`

验收：5 条样例均能离线执行并得到固定、合法的 Schema 和非空看板结果。

### FR-02 指令解析与规范化

- P0 使用确定性规则解析器（可称为 Mock Agent），不依赖在线大模型。
- 支持识别：股票、指标、最近 N 个交易日或明确日期范围、图表类型、Top N 极值标注。
- 支持同义词规范化，例如“股价/价格/收盘价”映射为 `close`，“交易量/成交量”映射为 `volume`，“涨跌/涨跌幅”映射为 `changePct`。
- 未明确图表类型时按指标选择默认值：`close`/`changePct` 使用折线图，`volume` 使用柱状图；同时包含两类指标时拆成两个图表，而非采用难以比较的同轴刻度。
- 未明确股票、指标或时间范围时不得暗中猜测关键业务字段；返回缺失字段错误和可复制的修正示例。
- 相同规范化输入和相同数据版本必须生成字段顺序及内容稳定的 Schema。

验收：固定样例快照测试多次运行结果一致；同义词输入生成相同的规范字段。

### FR-03 可视化 Schema

Schema 至少包含：

- `version`：Schema 版本。
- `requestId`：本次请求标识；测试中应可注入或固定，不能使快照随机失败。
- `stock`：股票代码和展示名称。
- `timeRange`：范围类型、起止日期、交易日数量及时区。
- `metrics`：指标标识、展示名、单位和聚合/计算方式。
- `charts`：图表 ID、类型、标题、X/Y 字段、关联指标和可选标注。
- `dataSource`：来源类型、名称、数据版本。
- `dataAsOf`：数据截止时间。
- `locale`：展示语言。

Schema 必须在数据查询和渲染之前进行运行时校验；界面提供可展开查看的格式化 JSON。

验收：Schema 缺少必填字段、枚举非法、日期倒置、未知指标或引用不存在指标的图表时，渲染器不执行，并展示字段级错误。

### FR-04 数据查询与指标计算

- 按股票和固定交易日期查询数据，`最近 N 天` 明确定义为截止日期（含）向前 N 个有数据的交易日，而非自然日。
- 原始字段至少包括 `date`、`open`、`high`、`low`、`close`、`volume`；P0 实际展示只要求 `close`、`volume` 和派生的 `changePct`。
- `changePct = (当日 close - 前一交易日 close) / 前一交易日 close × 100%`。
- 为计算范围内首日涨跌幅，可额外读取前一交易日作为计算上下文，但不得把该上下文日展示在图表中。
- 排序、筛选、Top N 和结论均基于同一份查询结果，避免图表和文字口径不一致。
- 内置数据必须是静态文件或由固定种子生成后提交的静态结果，不得使用当前时间或无种子随机数。

验收：数据结果按日期升序、数量符合范围定义、派生指标可通过固定期望值测试。

### FR-05 图表与看板

- P0 至少支持折线图和柱状图。
- 每张图必须有标题、坐标轴、单位、图例（多序列时）、Tooltip 和空状态。
- 图表配置只能由已校验 Schema 和标准化数据生成，禁止直接执行用户输入或任意配置片段。
- Top N 标注按指标值确定：`跌幅最大`取 `changePct` 最小的 N 个有效值，`涨幅最大`取最大的 N 个；并列时按日期升序保证稳定结果。
- 数值格式：价格保留 2 位小数，涨跌幅保留 2 位并带 `%`，成交量使用股数并可采用千/万缩写，但 Tooltip 应显示精确值。
- 图表需要在常见桌面宽度和窄屏下可读，容器尺寸改变后正确响应。

验收：至少两类图表能同时出现在一个看板；标注点可与表内源数据逐项核对。

### FR-06 结论生成

- 结论由确定性模板和当前查询结果生成，不调用在线模型。
- 结论只陈述数据事实，不给出买卖建议。
- 结论应包含股票、时间范围和一个可校验统计事实，例如区间收盘价变化、平均成交量或最大跌幅日期。
- 结论的数值格式、单位和舍入规则与图表 Tooltip 保持一致。
- 数据不足以支持结论时不生成推断性文字，改为说明“有效数据不足”。

验收：测试从同一数据计算结论预期值，不以脆弱的纯文案快照代替数值校验。

### FR-07 一次简单追问修改

- 仅在存在最近一次成功 Schema 时接受追问。
- P0 支持两类修改：替换指标（如“把成交量改成涨跌幅”）和替换时间范围（如“改为最近 10 天”）。
- 修改以不可变方式应用于上次 Schema，然后重新执行规范化、校验、查询和渲染全流程。
- 修改成功后记为新的当前 Schema；本项目不承诺继续理解第二次追问。
- 没有上下文、修改目标不存在、同时包含多个冲突修改或超出支持范围时，给出明确提示并建议提交完整新指令。

验收：题目给出的两条追问均可成功；无上次结果时输入追问不会崩溃且提示补充完整需求。

### FR-08 过程状态

状态机建议定义为：

`idle → understanding → schema_ready → querying_data → rendering → success`

任一步可转入 `error`；发起新请求后从 `understanding` 重新开始。为了让状态在本地 Demo 中可观察，可以使用可注入的短延迟；测试环境关闭延迟。不得伪装真实模型思考过程，只展示系统阶段和结果。

验收：用户能看到当前阶段、已完成阶段和失败阶段；状态不会停留在 Loading。

### FR-09 错误展示与恢复

- 错误区域包含：简短标题、可理解原因、修复建议、可选技术错误码。
- 预期业务错误不得展示原始堆栈。
- Error Boundary 捕获意外渲染异常，提供“重试/恢复到示例”操作。
- 新请求成功后清理旧错误；失败时保留用户输入和可用的上次成功看板。

建议错误码见第 11 节。

## 5. 非功能需求

### NFR-01 可运行性

- 建议使用当前 Node.js LTS，并在 `package.json` 声明支持版本；提交 lockfile 锁定依赖。
- 提供统一的 `install`、`dev`、`build`、`preview`、`test`、`lint` 命令。
- 默认运行不要求 API Key、账户登录、数据库或网络访问。
- README 从全新检出开始给出不超过数步的启动与测试说明。

### NFR-02 可复现性

- 模拟数据、数据截止日、Schema 版本、示例输入和预期输出全部进入版本控制。
- 禁止把 `Date.now()`、当前日期、随机 UUID 或无种子随机数用于决定业务输出；非业务请求 ID 应可注入。
- 日期解析固定使用 `Asia/Shanghai`，展示和测试不依赖机器本地时区。
- 排序和并列处理规则明确且稳定。

### NFR-03 可测试性

- 解析器、Schema 校验器、数据查询、指标计算、结论生成均设计为无 UI、无网络的纯函数或可注入服务。
- 图表配置生成与具体图表组件解耦，可直接断言 series、单位和标注。
- 最低必须有正常生成和 Schema 校验失败 2 个测试；项目建议按第 12 节覆盖更多关键分支。

### NFR-04 性能

- 针对内置小数据集，提交到可交互结果的目标时间小于 1 秒（不计演示用状态延迟）。
- 页面首次加载资源应保持轻量，避免引入后端、数据库和大型 Agent 框架。
- 图表只渲染查询范围内的数据，不进行无意义的全量重绘。

### NFR-05 可靠性与可解释性

- 所有外部边界都必须校验：用户输入、生成的 Schema、数据源结果、图表配置。
- 错误使用稳定错误码，日志保留内部原因，界面提供用户语言。
- 任何单次失败不能导致白屏或破坏上次成功状态。

### NFR-06 安全与隐私

- 不使用 `eval`、动态执行用户文本或将未经处理的 HTML 注入页面。
- 不收集个人信息；默认无遥测、无外部请求。
- 明示“数据为模拟数据，仅用于演示，不构成投资建议”。
- 若 P2 接入外部 Skill，凭据只能通过环境变量提供，不进入代码、日志或提交记录。

### NFR-07 可用性与可访问性

- 键盘可完成输入、示例选择、提交和错误恢复。
- 状态变化使用适当的可访问提示；按钮、输入和图表摘要具备可读标签。
- 不只依赖红/绿颜色表达涨跌；同时使用正负号、文字或图形标记。
- 桌面优先，同时保证约 375px 宽度下不横向破版。

### NFR-08 可维护性

- 开启 TypeScript 严格模式；领域模型不得散落为匿名对象。
- 业务规则集中在领域/服务层，页面组件不直接解析自然语言或计算金融指标。
- 数据源、解析器、渲染器通过明确接口隔离，便于替换但避免过度抽象。

## 6. 推荐技术架构

### 6.1 架构形式

采用“前端单体 + 分层模块 + 内置静态数据”的架构，不建设后端。理由是项目数据量小、无账户与持久化需求，前端即可满足离线运行、演示和测试，并显著降低环境复杂度。

逻辑数据流：

```text
用户输入/追问
    ↓
输入分类与规则解析器
    ↓
Schema 规范化 → 运行时 Schema 校验
    ↓
DataProvider（P0: LocalMockDataProvider）
    ↓
指标派生与分析（Top N、汇总）
    ↓
图表配置生成 + 结论生成
    ↓
看板 UI / Schema 面板 / 状态与错误面板
```

### 6.2 分层职责

- `UI/Presentation`：输入、样例、状态步骤、Schema 查看、图表、错误和免责声明。
- `Application`：编排完整用例，管理最近一次成功 Schema 和一次追问上下文。
- `Domain`：Schema、实体、指标定义、规则解析、校验、计算和结论模板。
- `Infrastructure`：模拟数据仓库、可选外部 Skill 适配器、图表库适配。

### 6.3 关键接口

- `IntentParser.parse(text, context?) -> ParseResult`
- `SchemaValidator.validate(candidate) -> ValidationResult<DashboardSchema>`
- `DataProvider.query(query) -> StockSeries`
- `MetricEngine.derive(series, metrics) -> EnrichedSeries`
- `ChartConfigBuilder.build(schema, data) -> ChartViewModel[]`
- `InsightGenerator.generate(schema, data, analysis) -> Insight`

各接口返回可辨识的成功/失败结果，而不是通过未分类异常表达可预期业务错误。

## 7. 推荐技术栈与语言

| 类别 | 推荐 | 原因 |
| --- | --- | --- |
| 语言 | TypeScript（严格模式） | Schema、指标和图表契约可静态检查，减少运行时字段错误 |
| 构建 | Vite | 本地启动快、配置少、适合单页笔试 Demo |
| UI | React | 组件生态成熟，状态和错误边界实现直接 |
| 图表 | Apache ECharts + `echarts-for-react` | 同时覆盖折线、柱状、标注、Tooltip 和响应式，扩展图表类型成本低 |
| Schema 校验 | Zod | 同一份定义提供运行时校验和 TypeScript 类型推导，错误路径清晰 |
| 日期 | date-fns（或仅小型自研 UTC/日期工具） | 处理固定日期范围，避免引入重量级依赖 |
| 测试 | Vitest + React Testing Library + jsdom | 与 Vite 配合好，可覆盖纯函数和用户流程 |
| 代码质量 | ESLint + Prettier | 统一静态检查和格式 |
| 包管理 | npm（提交 `package-lock.json`） | 环境普遍、评审启动成本低 |

不建议 P0 使用：Next.js/SSR、Node 后端、数据库、LangChain 等 Agent 框架或在线 LLM。它们不能增加本题核心验收价值，反而降低离线可复现性。

## 8. 建议目录结构

以下是后续实现阶段的建议，不在本阶段创建：

```text
.
├─ README.md
├─ TASK.md
├─ REQUIRE.md
├─ AGENT_LOG.md                    # 与 Agent 的关键对话/决策记录
├─ package.json
├─ package-lock.json
├─ vite.config.ts
├─ src/
│  ├─ app/
│  │  ├─ App.tsx
│  │  └─ useDashboardWorkflow.ts   # 状态机与用例编排
│  ├─ components/
│  │  ├─ QueryInput/
│  │  ├─ ExampleQueries/
│  │  ├─ ProcessSteps/
│  │  ├─ SchemaViewer/
│  │  ├─ Dashboard/
│  │  └─ ErrorPanel/
│  ├─ domain/
│  │  ├─ schema/
│  │  │  ├─ dashboardSchema.ts
│  │  │  └─ examples.ts
│  │  ├─ entities.ts
│  │  ├─ metrics.ts
│  │  ├─ errors.ts
│  │  └─ types.ts
│  ├─ services/
│  │  ├─ intentParser.ts
│  │  ├─ followUpParser.ts
│  │  ├─ dashboardOrchestrator.ts
│  │  ├─ metricEngine.ts
│  │  ├─ insightGenerator.ts
│  │  └─ chartConfigBuilder.ts
│  ├─ data/
│  │  ├─ stocks.json               # 股票主数据
│  │  ├─ prices/                   # 固定模拟日线数据
│  │  └─ dataManifest.json         # 版本、范围、截止时间、生成口径
│  ├─ infrastructure/
│  │  ├─ DataProvider.ts
│  │  ├─ LocalMockDataProvider.ts
│  │  └─ WenCaiSkillDataProvider.ts # P2，可选
│  ├─ styles/
│  └─ main.tsx
├─ tests/
│  ├─ fixtures/
│  │  ├─ queries.ts
│  │  └─ schemas/
│  ├─ unit/
│  └─ integration/
└─ public/
```

## 9. 领域实体与数据口径

### 9.1 核心实体

| 实体 | 关键字段 | 约束 |
| --- | --- | --- |
| `Stock` | `id`, `symbol`, `name`, `currency`, `exchange` | `symbol` 唯一；P0 至少内置 A/B/C 三家公司 |
| `PriceBar` | `stockId`, `date`, `open`, `high`, `low`, `close`, `volume` | 日期唯一且升序；价格 > 0；`volume` 为非负整数；OHLC 逻辑一致 |
| `MetricDefinition` | `id`, `label`, `unit`, `source/derive`, `precision` | `id` 只允许白名单枚举 |
| `TimeRange` | `mode`, `start`, `end`, `tradingDays`, `timezone` | `start <= end`；最近 N 日应设合理上限 |
| `ChartSpec` | `id`, `type`, `title`, `metricIds`, `xField`, `yFields`, `annotations` | 图表引用的指标必须存在于 Schema |
| `DashboardSchema` | `version`, `stock`, `timeRange`, `metrics`, `charts`, `dataSource`, `dataAsOf`, `locale` | 渲染前必须通过运行时校验 |
| `AnalysisResult` | `series`, `extrema`, `summary` | 全部来自同一规范化数据集 |
| `Insight` | `text`, `facts`, `disclaimer` | `facts` 保存机器可断言的结论依据 |
| `AppError` | `code`, `stage`, `message`, `suggestion`, `fieldPath?`, `cause?` | UI 不直接暴露 `cause` 和堆栈 |

### 9.2 指标白名单

| ID | 展示名 | 单位 | 来源/公式 | 推荐图表 |
| --- | --- | --- | --- | --- |
| `close` | 收盘价 | 元 | `PriceBar.close` | 折线图 |
| `volume` | 成交量 | 股 | `PriceBar.volume` | 柱状图 |
| `changePct` | 涨跌幅 | % | 相邻交易日收盘价计算 | 折线图或柱状图 |

P0 不接收 Schema 中任意自定义公式，防止错误计算和动态执行风险。

### 9.3 模拟数据要求

- 建议内置 3 只虚构股票，各不少于 60 个连续交易日数据，以覆盖 10/15/20/30 日及明确日期范围样例。
- 股票名称和代码清晰标记为模拟，例如 `A 公司 / MOCK-A`。
- 固定一个数据截止日期，例如 `2025-03-31T15:00:00+08:00`；所有“最近 N 天”均以此为默认截止点。
- 数据应包含上涨、下跌、成交量波动和足够的极值，便于验证 Top N。
- `dataManifest.json` 记录 `datasetVersion`、日期范围、时区、价格和成交量单位、生成方法与免责声明。

## 10. 输入与输出契约

### 10.1 输入

主请求：

```text
分析 A 公司最近 30 天的股价和成交量，并标出跌幅最大的 3 个交易日。
```

追问请求：

```text
把成交量改成涨跌幅
```

输入限制建议：

- 去除首尾空白后长度为 1～200 个字符。
- P0 仅支持中文及其中的 ASCII 股票代码/数字/日期。
- `最近 N 天` 中 N 建议限制为 2～60；Top N 建议限制为 1～10 且不得超过有效数据数。
- 明确日期使用 `YYYY-MM-DD`；结束日期晚于数据截止日时截断并告知用户，开始日期无数据时按空数据处理而非伪造。

### 10.2 Schema 输出示例

以下仅为契约示意；最终样例中的日期应由固定数据集精确确定：

```json
{
  "version": "1.0",
  "requestId": "demo-001",
  "stock": {
    "symbol": "MOCK-A",
    "name": "A 公司"
  },
  "timeRange": {
    "mode": "lastTradingDays",
    "start": "2025-02-18",
    "end": "2025-03-31",
    "tradingDays": 30,
    "timezone": "Asia/Shanghai"
  },
  "metrics": [
    { "id": "close", "label": "收盘价", "unit": "元" },
    { "id": "volume", "label": "成交量", "unit": "股" },
    { "id": "changePct", "label": "涨跌幅", "unit": "%" }
  ],
  "charts": [
    {
      "id": "price-trend",
      "type": "line",
      "title": "A 公司最近 30 个交易日收盘价",
      "metricIds": ["close"],
      "xField": "date",
      "yFields": ["close"],
      "annotations": [
        { "kind": "bottomN", "metricId": "changePct", "count": 3 }
      ]
    },
    {
      "id": "volume-bars",
      "type": "bar",
      "title": "A 公司最近 30 个交易日成交量",
      "metricIds": ["volume"],
      "xField": "date",
      "yFields": ["volume"],
      "annotations": []
    }
  ],
  "dataSource": {
    "type": "mock",
    "name": "Built-in Mock Market Data",
    "datasetVersion": "1.0.0"
  },
  "dataAsOf": "2025-03-31T15:00:00+08:00",
  "locale": "zh-CN"
}
```

说明：`changePct` 即使不单独画图，也因极值标注计算需要出现在 `metrics` 中。展示指标与辅助计算指标可在后续 Schema 版本中通过 `role` 字段进一步区分。

### 10.3 成功输出

- 已校验的格式化 Schema。
- 一个或多个图表 ViewModel 及实际图表。
- 一条结论及免责声明。
- 数据源、版本和截止时间。
- 完整流程状态为 `success`。

### 10.4 失败输出

```json
{
  "ok": false,
  "error": {
    "code": "UNKNOWN_METRIC",
    "stage": "schema_validation",
    "message": "暂不支持指标“市盈率”。",
    "suggestion": "可使用：收盘价、成交量、涨跌幅。",
    "fieldPath": "metrics[0].id"
  }
}
```

## 11. 边界、异常与恢复策略

| 场景 | 系统行为 | 建议错误码 |
| --- | --- | --- |
| 空输入/仅空白 | 不提交，输入框就地提示 | `EMPTY_QUERY` |
| 输入超过长度 | 拒绝并告知上限 | `QUERY_TOO_LONG` |
| 无法识别股票 | 列出可用模拟股票 | `UNKNOWN_STOCK` |
| 无法识别指标 | 列出支持指标 | `UNKNOWN_METRIC` |
| 缺少关键字段 | 指出缺失项并给完整示例 | `MISSING_REQUIREMENT` |
| N 非数字、过小或过大 | 说明允许范围 | `INVALID_RANGE` |
| 起止日期倒置 | 不查询数据，指出正确格式 | `INVALID_DATE_RANGE` |
| 日期超出数据范围 | 未来结束日可截断并提示；完全无重叠则报空数据 | `RANGE_OUT_OF_DATASET` |
| 合法范围但无记录 | 显示空状态，不渲染空坐标系 | `NO_DATA` |
| 计算涨跌幅缺少前值 | 该点记为不可用；全范围不足则说明有效数据不足 | `INSUFFICIENT_DATA` |
| Schema 结构错误 | 展示字段路径和期望类型，阻止渲染 | `SCHEMA_INVALID` |
| 图表引用未知字段 | 阻止该看板渲染，指出错误图表 | `CHART_CONFIG_INVALID` |
| 无上下文直接追问 | 要求先生成看板或提交完整指令 | `FOLLOW_UP_WITHOUT_CONTEXT` |
| 追问含冲突修改 | 不猜测，提示一次只修改指标或范围 | `AMBIGUOUS_FOLLOW_UP` |
| 快速连续提交 | 取消/忽略旧请求，只接受最后请求结果 | `REQUEST_SUPERSEDED` |
| 图表组件意外异常 | Error Boundary 显示恢复操作，记录内部错误 | `RENDER_FAILED` |
| 可选外部数据源失败 | 明确提示；按配置回退本地数据并标注来源 | `DATA_SOURCE_UNAVAILABLE` |

通用恢复原则：解析或校验错误不得发起数据查询；数据错误不得生成误导性图表或结论；渲染错误不得使整个页面白屏。

## 12. 测试与验收策略

### 12.1 P0 自动化测试

题目只强制 2 个测试，但为降低笔试演示风险，建议至少覆盖：

1. 正常主流程：样例 1 生成合法 Schema、两个图表和一致结论。
2. Schema 校验失败：缺少 `dataAsOf` 或使用未知指标时返回字段级错误且不渲染。
3. 解析器参数化测试：5 条示例分别得到期望股票、指标、范围和图表类型。
4. 指标计算：固定两日收盘价计算正确的 `changePct`。
5. Top N：最大跌幅 3 日结果、顺序和并列规则正确。
6. 追问修改：成交量替换为涨跌幅；30 日替换为 10 日。
7. 空数据：显示 `NO_DATA`，页面和上次结果可恢复。
8. UI 集成：状态经过预期阶段并最终显示 Schema、图表容器和结论。

### 12.2 手工验收清单

- 全新环境可依 README 启动，无外部账户/API Key。
- 5 条示例均可点击运行，刷新后结果一致。
- 至少折线、柱状两种图表可见且单位正确。
- Schema 中包含题目规定的全部字段。
- 图表数据、Top N 标记和结论可互相核对。
- 两类追问均成功，无上下文追问有清晰错误。
- 非法指标、非法 Schema 和空数据不会导致白屏。
- 窄屏可操作，键盘可提交，免责声明可见。
- `test` 和 `build` 命令均通过。
- 提交物包含源码、模拟数据、Schema 样例、测试、README 和 Agent 对话记录。

## 13. 开发阶段与优先级

### P0：必须完成，决定是否可交付

#### 阶段 0：契约冻结与数据准备

- 冻结指标白名单、Schema v1、错误码和日期口径。
- 准备 3 只股票的固定数据集与 manifest，验证 OHLC、唯一日期和数据长度。
- 固化 5 条示例及对应期望 Schema fixture。

完成标准：数据与契约可以独立测试，任何人能解释“最近 N 天”和涨跌幅算法。

#### 阶段 1：领域核心

- 实现确定性主指令解析和同义词规范化。
- 实现 Zod Schema、业务交叉校验、数据查询、指标派生、Top N 和结论生成。
- 先完成纯函数单元测试，再接 UI。

完成标准：无浏览器、无网络即可从固定输入得到固定分析结果；非法输入有结构化错误。

#### 阶段 2：看板主流程

- 实现输入、示例、状态机、Schema 面板、折线图、柱状图、结论和免责声明。
- 接入流程编排与 Error Boundary。

完成标准：样例 1 完整走通；图表、Schema 和结论数据一致；失败不白屏。

#### 阶段 3：追问与交付质量

- 实现两类一次追问。
- 完成 5 条样例、正常/失败集成测试、响应式和可访问性基本检查。
- 编写 README、实现说明、数据口径、已知限制和 Agent 对话记录。
- 验证全新安装、测试、构建与预览命令。

完成标准：第 12.2 节手工验收清单全部通过。

### P1：强烈建议，提升完成度

- 增加组合图、面积图或涨跌幅正负柱状图，但仍复用相同数据契约。
- Schema JSON 复制/下载、输入历史（仅当前会话）。
- 更丰富的规则表达：`过去一个月`、股票代码、图表类型显式指定。
- 图表下方提供可访问的数据摘要表。
- 增加数据质量校验脚本、测试覆盖率阈值和端到端冒烟测试。
- 更细致的加载骨架、Tooltip、动画和移动端布局。

### P2：可选探索，不影响核心验收

- 问财 Skill Hub 数据适配器及本地回退策略。
- 在线 LLM 解析器，但必须保留规则解析器作为离线默认，并对模型输出执行同一 Schema 校验。
- 多股票比较、更复杂统计指标或更多追问类型。
- 看板导出图片/JSON。

优先级纪律：P0 未通过自动测试和全新环境启动验证前，不投入 P1/P2。

## 14. 复杂任务处理策略

### 14.1 一条指令包含多个指标

将复杂请求拆为一个共享的 `DashboardSchema` 和多个 `ChartSpec`。价格与成交量默认分图展示，复用同一时间范围和数据查询结果。辅助指标（例如为跌幅标注所需的 `changePct`）加入指标依赖，但不强制单独画图。

### 14.2 极值标注

处理顺序固定为：读取额外前值 → 计算全范围涨跌幅 → 丢弃上下文日 → 过滤无效值 → 按值排序 → 以日期升序处理并列 → 截取 N 项 → 映射到目标图表日期。N 超出有效数据量时标注所有有效点并给出非阻断提示。

### 14.3 明确日期与非交易日

开始/结束日期落在周末或数据缺失日时，查询该闭区间内实际存在的交易日，不人为补值；界面展示最终实际起止交易日。区间完全无数据时返回 `NO_DATA`。

### 14.4 歧义输入

对关键字段存在多个候选或互相冲突时不静默猜测。P0 不建设交互式澄清对话，而是返回“无法唯一理解”的错误，列出检测到的冲突以及推荐完整表达。

### 14.5 在线能力扩展

未来接入模型或问财 Skill 时，外部输出都先转换为内部候选 Schema，再执行相同的白名单、运行时和业务校验。设置超时和回退；外部错误不得污染最近一次成功状态，数据来源必须在看板上可见。

## 15. 技术风险与缓解措施

| 风险 | 影响 | 概率 | 缓解措施 |
| --- | --- | --- | --- |
| 自然语言规则覆盖不足 | 示例外表达解析失败 | 中 | 明确 P0 语法范围；维护同义词表和参数化测试；错误中提供可用表达 |
| 规则过度猜测 | 生成错误但看似合理的看板 | 中 | 股票/指标/范围为必填；冲突时报错；展示 Schema 供核对 |
| 时间范围口径不一致 | 图表数量和结论错误 | 高 | 固定数据截止日、时区和“交易日”定义；统一日期服务并测试边界 |
| 图表、标注、结论各算一遍 | 三者数据不一致 | 高 | 单一查询和分析结果作为唯一事实源，所有输出只消费该结果 |
| Schema 只有类型无运行时校验 | 非法配置导致白屏 | 高 | 使用 Zod 加业务交叉校验，校验通过后才能查询和渲染 |
| ECharts 在 jsdom 中难测 | UI 测试脆弱 | 中 | 单测 Chart ViewModel；组件测试 Mock 图表库；少量浏览器冒烟验证 |
| 当前时间/随机数进入结果 | 快照和演示不可复现 | 中 | 固定截止日和数据版本；时钟、ID、延迟可注入；测试禁用动画/延迟 |
| 可选问财 Skill 不稳定或不可用 | 无法离线启动或演示 | 高 | 本地数据为默认；适配器隔离；超时、回退和来源标注；不作为 P0 依赖 |
| 为展示“Agent”引入复杂框架 | 工期和环境风险上升 | 中 | P0 明确采用可解释的规则 Mock Agent；把时间投入 Schema 与一致性 |
| 指标单位/双轴误导 | 用户误读 | 中 | 不同量纲默认分图；标题、轴、Tooltip 和结论都标单位 |
| 快速提交产生竞态 | 旧请求覆盖新结果 | 中 | 请求序号或取消令牌；只提交最新请求状态 |
| 内置数据质量差 | 极值或公式演示失真 | 中 | manifest + 数据校验测试；人工核对至少一个完整样例 |

## 16. 交付定义（Definition of Done）

只有同时满足以下条件，P0 才视为完成：

- 所有“必须完成”需求都有实现和对应验收证据。
- 默认路径完全离线，不需要私密配置。
- 5 条样例、Schema 定义和对应结果可复现。
- 正常生成与 Schema 校验失败测试存在且通过，建议测试集全部通过。
- 生产构建通过，页面无未处理异常或控制台关键错误。
- 图表、标注和结论使用同一数据口径，至少抽查一个样例数值一致。
- README 包含环境、启动、测试、架构、数据口径、限制和免责声明。
- Agent 对话/关键决策记录随项目提交，且不包含凭据或隐私数据。

## 17. 待实现前确认项

以下事项不阻塞本文档，但应在编码前冻结并记录到 README/ADR：

1. 固定模拟数据的最终截止日期和精确日期覆盖范围。
2. A/B/C 三家模拟公司的最终代码、币种和数据生成方式。
3. 图表库是否采用 `echarts-for-react` 包装层，或自行封装 ECharts 生命周期。
4. P1 是否加入第三类图表；P0 不应因此延后。
5. 问财 Skill Hub 是否具备可公开复现的调用条件；若不具备，保持 P2 并明确限制。

