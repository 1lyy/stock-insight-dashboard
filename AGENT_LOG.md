# Agent 协作记录

本文档记录本项目与编码 Agent 的主要协作阶段、用户要求、关键决策和验证结果。它是提交材料的一部分，不包含凭据或隐私数据。

## 2026-09-21：需求分析

用户要求 Agent 以项目经理身份阅读 `TASK.md`，暂不编写代码，整理功能需求、非功能需求、范围约束、技术架构、目录、技术栈、实体、输入输出、阶段、优先级和风险。

Agent 产出：

- 创建 `REQUIRE.md`。
- 将“可运行、可复现、可测试、错误可解释”作为优先顺序。
- 决定 P0 使用固定本地模拟数据；问财 Skill Hub 作为非必需的 P2 适配器。
- 建议 React + TypeScript + Vite + Zod + ECharts + Vitest。

## 2026-09-21：阶段 0——契约冻结与数据准备

用户要求创建基础项目，冻结 Schema、指标、错误码和日期口径，提供三只股票静态数据、5 个样例 fixture 和基础测试。

Agent 产出与决策：

- 创建 React + TypeScript + Vite 项目并开启严格类型检查。
- 定义 `DashboardSchema v1`，使用 Zod 进行运行时和业务交叉校验。
- 冻结 `close`、`volume`、`changePct` 指标。
- 固定数据版本 `1.0.0`、时区 `Asia/Shanghai`、截止时间 `2025-03-31T15:00:00+08:00`。
- 提交 MOCK-A/B/C 各 63 条静态日线数据，不在运行时随机生成。
- 固化 5 条自然语言指令及预期 Schema JSON。
- 验证类型检查、测试和生产构建。

## 2026-09-21：阶段 1——领域核心

用户要求实现确定性意图解析、Mock 查询、涨跌幅/极值、确定性结论和单元测试。

Agent 产出与决策：

- `IntentParser` 采用确定性规则和有限同义词表，不调用在线模型。
- `LocalMockDataProvider` 支持最近 N 个交易日和明确日期闭区间。
- 查询范围首日的涨跌幅使用额外的前一交易日上下文计算。
- `MetricEngine` 统一计算涨跌幅和涨跌幅/成交量 Top N、Bottom N。
- `InsightGenerator` 只从实际查询结果生成文案和机器可读 facts。
- `AnalyzeQuery` 将解析、校验、查询、计算和结论编排为一个无 UI 用例。
- 测试确认 5 条输入生成结果与冻结 fixture 一致。

## 2026-09-21：阶段 2——Dashboard 主流程

用户要求实现输入、5 条示例、阶段状态、Schema Viewer、ECharts、结论、错误提示和 Error Boundary，不实现追问。

Agent 产出与决策：

- 页面展示真实领域执行节点的四阶段状态。
- UI 复用 `AnalyzeQuery` 输出，不重复业务计算。
- 使用 ECharts Core 按需注册折线图、柱状图、Tooltip、缩放和标注。
- 增加业务错误面板和 React Error Boundary。
- 增加响应式布局、键盘提交和 UI 集成测试。
- 尝试通过 Computer Use 做本地浏览器视觉验收；执行环境没有暴露可用浏览器，因此以生产构建和 jsdom UI 测试作为本阶段验证证据。

## 2026-09-21：阶段 3——追问修改与交付完善

用户要求支持一次简单追问、重新执行完整分析链路、处理追问异常，并完善交付材料。

Agent 产出与决策：

- 新增 `FollowUpParser`，只允许指标替换或最近 N 日范围修改。
- `把成交量改成涨跌幅` 会更新指标、图表字段、默认图表类型、标题和相关标注。
- `改为最近 10 天` 会从固定交易日历重新计算起止日期并更新所有图表标题。
- 修改后的候选 Schema 必须再次通过 Zod 和业务交叉校验，再查询、计算、生成结论和渲染。
- 无上下文、多个修改、不支持修改、目标不存在和次数超限都返回结构化错误。
- UI 只保留最近一次成功 Schema，失败追问不破坏现有看板；成功追问后锁定本轮上下文。
- 补充领域与 UI 集成测试，并完善 README、Schema 示例、数据口径、限制和本协作记录。

## 通用质量策略

- 所有业务结果都来自固定版本数据，不依赖当前时间、随机数或网络。
- Schema 是数据查询和渲染前的强制边界。
- 图表、极值和 Insight 使用同一份 `AnalysisOutput`。
- 预期业务错误使用错误码、用户语言和修复建议；未知渲染异常由 Error Boundary 兜底。
- 每个阶段结束均运行 `typecheck`、`test` 和 `build`，发现问题后修复并重新验证。

## 2026-09-21：最终验收与代码 Review

用户要求按 `REQUIRE.md` 检查完整性、数据一致性、异常边界、重复逻辑和提交材料，并直接修复问题。

Agent 完成：

- 增加跨 5 条样例的端到端验收测试，逐项复算图表序列、极值和 Insight facts。
- 收紧 Schema 与静态数据不变量：股票映射、唯一标识、图表字段、指标引用、标注能力、序列归属和日期顺序。
- 完善歧义指令、混合未知指标、非交易日截止、Top N 数量不足、空数据及未预期异常处理。
- 提取完整指令与追问共同使用的 Schema 命名和指标构造逻辑，消除领域层重复。
- 为涨跌幅结论补充机器可验证 fact，为图表 tooltip 和无障碍摘要补充明确单位。
- 增加 ESLint 配置和 `lint` 脚本，并以 `lint`、`typecheck`、全部测试和生产构建作为最终交付门禁。
- 最终验证结果：ESLint 零 warning、TypeScript strict 检查通过、10 个测试文件共 77 项测试通过、生产构建通过；生产预览返回 HTTP 200。

## 2026-09-21：P2——问财 Skill Hub 探测与回退适配

用户要求先真实确认 Skill、参数和返回结构；只有稳定可调用时才接入，无法验证时不得伪造。

Agent 核验与决策：

- 通过官方文档确认 `hithink-finance-a-share`、`get_a_share_prices_historical`、请求参数和成功响应字段契约。
- 插件目录未发现问财/同花顺插件；当前环境也没有对应 MCP 工具或 API Key。
- 对官方 REST 等价端点执行一次无凭据最小探测，实际收到 HTTP 200、业务错误 `code=2003` 和 `Missing X-api-key`，只证明端点及鉴权边界存在，不能证明真实行情权限可用。
- 发现当前 Schema 的 `MOCK-A/B/C` 与官方接口要求的真实 `thscode` 不兼容；为避免把真实股票数据冒充模拟公司数据，没有构造隐式映射。
- 新增明确未连接的 `WenCaiSkillDataProvider` 适配边界和 `FallbackDataProvider`，外部能力不可用时自动回退固定 Mock，并在查询结果及看板上标记实际来源。
- 最终验证结果：ESLint、TypeScript strict 和生产构建通过，11 个测试文件共 79 项测试通过。

## 2026-09-21：最终交付验收

用户要求不新增业务功能，对真实浏览器、全新安装、提交清洁度和演示流程进行最终确认。

验收结果：

- Computer Use 会话未暴露可用浏览器窗口；改用机器已安装的真实 Chrome，通过 DevTools Protocol 完成同等只读验收，没有把 jsdom 当作浏览器结果。
- 在 1440×1000 和 375×900 视口检查页面；桌面双图布局、窄屏单列布局均无横向溢出。
- 逐条运行 5 条样例，页面 Schema 均与冻结 fixture 深度一致；图表数量、标题、单位、Insight 和实际来源一致。
- 检查折线图、柱状图、三个极值 pin 和 Tooltip；Tooltip 显示具体日期、`24.07 元` 等精确单位值。
- 分别从样例 1 执行两条追问，指标、时间范围、图表标题和 Insight 均同步更新。
- `UNKNOWN_METRIC` 错误面板可读，且保留上一次成功 Insight 与看板；浏览器控制台无未处理异常。
- 在独立副本按 README 完成 `npm install`、`npm run dev`（HTTP 200）、`npm test`（79/79）和 `npm run build`。
- 清理所有验收截图、临时浏览器依赖/profile 和全新安装副本；源码扫描未发现调试日志、跳过测试、TODO/FIXME、随机业务输出或临时文件。
