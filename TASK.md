一句话生成股票分析看板

一、题目内容

设计并实现一个简化的“自然语言生成股票分析看板”。用户输入一句分析需求，系统将其转换为结构化可视化 Schema，并根据 Schema 生成股票分析看板。
1. 优先通过[问财 Skill Hub](https://www.iwencai.com/skillhub)中的 Skill 获取看板的数据
2. 看板需要包含丰富的可视化图表类型来展示分析的结果

示例输入：

分析 A 公司最近 30 天的股价和成交量，并标出跌幅最大的 3 个交易日。

项目自行内置最小、可复现的模拟股票数据，无需使用真实金融数据或接入外部行情接口。

二、必须完成

1）提供自然语言输入框，并支持至少 5 条不同的示例指令。
2）将用户指令转换为结构化可视化 Schema。可以调用模型，也可以使用规则或模拟 Agent 实现，但需保证输出可复现。
3）Schema 至少描述：股票标识、指标、时间范围、图表类型、标题、数据来源、数据截止时间和单位。
4）根据 Schema 渲染至少 2 类图表，例如折线图和柱状图，并展示一条与图表数据一致的简短结论。
5）支持一次简单追问修改，例如“把成交量改成涨跌幅”或“改为最近 10 天”。
6）展示“理解需求、生成 Schema、渲染图表”等过程状态。
7）Schema 不合法、指标不存在或数据为空时，展示可理解的错误提示，不得导致页面崩溃。

三、范围约束

项目必须自包含全部运行数据。不要求真实行情、复杂 K 线、多个数据源、自由拖拽布局、多轮对话记忆或生产级流式服务。

四、候选人提交要求

需要使用工具：Codex、Claude Code 或者其他的 Agent 工具。
• 可运行的本地 Demo 及完整源码。
• 完成项目过程中和Agent对话的记录
• 项目内置的模拟股票数据。
• 至少 5 条自然语言生成样例（mock的输入以及对应agent的输出看板结果）。
• 可视化 Schema 定义及示例。
• 至少 2 个测试，覆盖正常生成和 Schema 校验失败。
• README，说明启动方式、实现方案、数据口径和已知限制。